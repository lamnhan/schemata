import { PromotionsPipe } from './promotions.pipe';

describe('PromotionsPipe', () => {
  it('create an instance', () => {
    const pipe = new PromotionsPipe();
    expect(pipe).toBeTruthy();
  });
});
